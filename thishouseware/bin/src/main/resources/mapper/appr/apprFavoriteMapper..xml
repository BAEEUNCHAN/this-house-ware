<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.contractor.app.appr.mapper.apprFavoriteMapper.">

<!-- 전체조회 -->
<select id="apprFavoriteAllList" resultType="ApprFavoriteVO">
		SELECT favorite_no
        	, favorite_name
        	, favorite_link
        	, create_dt
        	, update_dt
		FROM approval_line_favorite
		<where>
			<if test="favoriteNo != null and !favoriteNo.equals('')">
				favorite_no = #{favoriteNo}
			</if>
			<if test="favoriteName != null and !favoriteName.equals('')">
				favorite_name = #{favoriteName}
			</if>
			<if test="favoriteLink != null and !favoriteLink.equals('')">
				favorite_link = #{favoriteLink}
			</if>
		</where>
		ORDER BY create_dt
	</select>


</mapper>