<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.contractor.app.appr.mapper.apprLineMapper.">
<!-- 전체조회 -->
<select id="apprLineAllList" resultType="ApprLineVO">
		SELECT approval_line_no
        	, id
        	, approval_line_name
        	, create_dt
        	, resent_user_dt
        	, referrer
        	, favoriteChk        	
		FROM approval_line
		<where>
			<if test="approvalLineNo != null and !approvalLineNo.equals('')">
				approval_line_no = #{approvalLineNo}
			</if>
			<if test="id != null and !id.equals('')">
				id = #{id}
			</if>
			<if test="approvalLineName != null and !approvalLineName.equals('')">
				approval_line_name = #{approvalLineName}
			</if>
		</where>
		ORDER BY create_dt
	</select>

</mapper>